server {
    listen 80;
    listen [::]:80;
    server_name ${EDGE_DOMAIN} ${SUB_DOMAIN};

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/edge-infra-acme;
        default_type text/plain;
    }

    location / {
        return 404;
    }
}
